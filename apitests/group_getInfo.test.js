const expect = require('expect');
const testUtil = require('./testutil');

const errors = require('../helpers/errorstousers');
const success = require('../helpers/successtousers');

beforeAll(async () => {
    console.log = () => { };
});

beforeEach(() => { });

afterEach(() => { });

afterAll(() => { });

describe('getInfo', () => {
    test('should fail, if groupids is undefined', async () => {

    });

    test('should fail, if groupids is null', async () => {

    });

    test('should fail, if groupids is not an array', async () => {

    });

    test('should fail, if groupids has string in it', async () => {

    });

    test('should fail, on error', async () => {

    });

    test('should return groups info', async () => {

    });
});
