const expect = require('expect');
const testUtil = require('./testutil');

const errors = require('../helpers/errorstousers');
const success = require('../helpers/successtousers');

beforeAll(async () => {
    console.log = () => { };
});

beforeEach(() => { });

afterEach(() => { });

afterAll(() => { });

describe('getInfo', () => {
    test('should fail, if userids is not an array', async () => {

    });

    test('should fail, if userids array contains string', async () => {

    });

    test('should fail, if userids array has empty values', async () => {

    });

    test('should fail, on error', async () => {

    });

    test('should return userinfo for the userids', async () => {

    });
});