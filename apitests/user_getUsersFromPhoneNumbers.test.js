const expect = require('expect');
const testUtil = require('../testutil/testUtil');

const errors = require('../helpers/errorstousers');
const success = require('../helpers/successtousers');

beforeAll(async () => {
    console.log = () => { };
});

beforeEach(() => { });

afterEach(() => { });

afterAll(() => { });

describe('getUsersFromPhoneNumbers', () => {
    test('should fail, if phoneNumbers is not an array', async () => {

    });

    test('should fail, if phoneNumbers is an array numbers', async () => {

    });

    test('should fail, if phoneNumbers array has empty string', async () => {

    });

    test('should fail, on error', async () => {

    });

    test('should return userinfo for the available phoneNumbers', async () => {

    });
});